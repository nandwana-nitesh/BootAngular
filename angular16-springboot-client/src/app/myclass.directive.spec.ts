import { MyclassDirective } from './myclass.directive';

describe('MyclassDirective', () => {
  it('should create an instance', () => {
    const directive = new MyclassDirective();
    expect(directive).toBeTruthy();
  });
});
